# ==================================
# My Social Networks API
# Configuration des variables d'environnement
# ==================================

# Port du serveur
PORT=3000

# URI de connexion MongoDB
# Format local: mongodb://localhost:27017/nom_base
# Format Atlas: mongodb+srv://user:password@cluster.mongodb.net/nom_base
MONGODB_URI=mongodb://localhost:27017/my-social-networks

# Secret pour les tokens JWT
# IMPORTANT: Changez cette valeur en production !
# Générez une chaîne aléatoire longue et sécurisée
# Exemple: openssl rand -base64 64
JWT_SECRET=votre_secret_jwt_tres_long_et_tres_securise_a_changer_absolument_en_production

# Durée de validité des tokens JWT
# Exemples: 1h, 7d, 30d, 90d
JWT_EXPIRE=7d

# Environnement
# development | production | test
NODE_ENV=development

# ==================================
# Configuration optionnelle
# ==================================

# Limite de taille pour les uploads (en MB)
# MAX_FILE_SIZE=10

# URL de base de l'API (pour les liens dans les emails par exemple)
# API_URL=http://localhost:3000

# Configuration email (si implémentation future)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=votre.email@example.com
# SMTP_PASS=votre_mot_de_passe

# ==================================
# NOTES IMPORTANTES
# ==================================

# 1. NE JAMAIS commiter le fichier .env sur Git
#    (il est déjà dans .gitignore)

# 2. Copiez ce fichier vers .env et modifiez les valeurs :
#    cp .env.example .env

# 3. En production, utilisez des secrets forts :
#    - JWT_SECRET doit être une longue chaîne aléatoire
#    - MongoDB Atlas pour la base de données cloud
#    - HTTPS activé

# 4. Pour générer un JWT_SECRET sécurisé :
#    node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
#    ou
#    openssl rand -base64 64
